# 작업 이력 — 2026-07 · HIS

> 시간순(최신 위). 항목: `### [분류] 제목` + 증상/원인/조치/변경 파일/검증/관련.
> 안내/규칙: [../작업이력.md](../작업이력.md) · 주제별 상세: [../troubleshooting/](../troubleshooting/README.md)

---

## 2026-07-08

### [리팩터] 라우터 영역(대민/관리자)→도메인 2단계 모듈 분리
- 배경: `router/index.js` 한 파일에 17개 라우트·151줄이 몰려 있었음(도메인 늘며 비대). 참조 프레임워크(spring_boot_vue3)는 modules로 분리해 조합.
- 조치: 최상위를 영역(대민/관리자)으로, 그 아래를 도메인 모듈로 나눔.
  · `router/index.js`(createRouter+가드+조합), `router/public/index.js`(대민 /, /auth), `router/admin/index.js`(/admin/login + AdminLayout + 도메인 children 조합), `router/admin/{client,contract,quote}Routes.js`(도메인별 children 배열).
  · 새 도메인 추가 = 모듈 1개 + admin/index.js 에 `...xxxRoutes` 한 줄.
- 변경 파일: `frontend/vue/src/router/index.js`(슬림화), `router/public/index.js`·`router/admin/*`(신설 5)
- 검증: 라우트 name·path·meta·props 전부 동일(파일 이동만) → 화면 코드 무변경. vite build SUCCESS(모듈 import 정상·지연로딩 청크 유지).

### [환경] SPA 서빙 경로 /app → 루트(/) 전환 (대민 /, 관리자 /admin)
- 배경: 컨텍스트루트가 /app 이라 URL 이 /app/, /app/admin 이었음. 고객 대면 제품엔 루트 서빙이 자연스러워 전환(출시 전이라 변경비용 최소).
- 조치:
  · 프론트: vite `base '/app/'→'/'`, `outDir static/app→static`(루트). 소스 이동 없음(설정만) — 산출물이 static/ 루트(index.html·assets·favicon)로 나옴.
  · 백엔드 WebConfig: 리소스 핸들러를 `/app/**` → `/**` SPA 폴백으로 변경(정적파일 있으면 서빙, 없으면 index.html). **백엔드 경로(api/·actuator/·ws-chat·error)는 폴백 제외**(isBackendPath)해 미존재 API 가 SPA HTML 로 200 되지 않게 함. /app 뷰컨트롤러 제거, 인터셉터 제외를 /assets/**·/favicon.svg·/error 로 갱신.
  · 로그인 리다이렉트: LoginEntryController 기본값·etc.properties `auth.login-redirect` /app→/.
  · .gitignore: `static/app/` → `static/`(빌드산출물 전체). CLAUDE.md·STATUS·주석 현행화.
- 변경 파일: `frontend/vue/vite.config.js`, `config/WebConfig`, `auth/controller/LoginEntryController`, `etc.properties`, `.gitignore`, 문서/주석
- 검증: clean compile + vite build SUCCESS. 실기동 HTTP 라우팅 E2E — /·/admin/login·/admin/clients→200 SPA, /admin(미인증)→302 /admin/login·(세션인증)→200, /assets/*.js·/favicon.svg→200 정상타입, /api/**(미인증)→401 JSON, **인증된 미존재 /api/**→404 JSON(HTML 아님)**, /login→302 /, 세션로그인·인증 API 정상.
- 참고: WebSocket STOMP prefix `/app`(setApplicationDestinationPrefixes)은 HTTP 아닌 메시지 라우팅이라 무관. SPA `/auth` 데모 라우트는 백엔드 /auth 컨트롤러가 우선(공존).

### [기능] 견적(quote) 도메인 추가 — 일회성 특수청소 견적, 관리자 CRUD
- 조치: 세 번째 도메인 수직 슬라이스(백→프론트). 계약(contract) 템플릿을 따르되, 일회성 특수청소(입주·물탱크 등) 성격에 맞춰 설계.
  · 설계 확정: 거래처(client) 연결 선택(nullable) + 고객 정보(고객명·연락처·주소) 직접 입력(신규 일회성 고객 대응). 상태 PENDING/ACCEPTED/REJECTED(대기/수락/거절). 금액은 1회 견적금액.
  · 백엔드(`com.boot.cleanhub.quote`): Quote 엔티티(@ManyToOne(LAZY)→Client nullable)·QuoteStatus·Flyway V7__create_quote.sql(client_id FK **ON DELETE SET NULL**+인덱스)·QuoteRepository(left join fetch — 연결 없는 견적도 조회, title/customerName 검색)·QuoteService(CRUD·거래처 선택 연결·상태 기본 PENDING·수정 시 saveAndFlush)·DTO(요청 검증: @AssertTrue 고객식별 최소보장·유효기간>=견적일 교차검증 / 응답 평면화)·QuoteAdminController(/api/admin/quotes)·ErrorCode.QUOTE_NOT_FOUND(100201)+i18n.
  · 프론트: quoteService(+QUOTE_STATUSES)·목록(검색·금액 원단위·상태 tag·행클릭)·등록/수정 폼(거래처 선택+고객 직접입력, watch 로 수정 로드, 유효기간/고객식별 가드)·상세, 라우터 /admin/quotes** 4개·사이드바·대시보드 카드.
  · 거래처 삭제 시: 견적은 남기고 연결만 해제(ON DELETE SET NULL) — 견적에 고객 정보가 보존되므로 안전. (계약과 달리 거래처 삭제를 막지 않음)
- 변경 파일: `quote/*`(신설 7: domain 2·repository·dto 2·service·controller), `error/ErrorCode`·i18n(ko/en), `db/migration/V7__create_quote.sql`, 프론트 `views/admin/quote/*`(신설 3)·`services/quote/quoteService.js`·`router/index.js`·`layouts/AdminLayout.vue`·`views/admin/AdminDashboardView.vue`
- 검증: clean compile + vite build SUCCESS. 실기동 E2E — V7 적용·validate 통과, 거래처연결/신규고객 견적 등록, 고객식별 없음 400·유효기간<견적일 400, title/고객명 검색, 수정(updatedAt 갱신·상태변경), **거래처 삭제 시 견적 잔존+clientId=null(SET NULL)**, 삭제 후 404.
- 관련: 로드맵상 다음은 정산(settlement) — 계약+견적 집계·월 청구·엑셀(아버님 양식 미수령).

### [수정] 코드리뷰 지적 처리 — P1-2/P2-1/P2-2 (review-contract-2026-07-08.md)
- 조치:
  · P1-2(종료일<시작일 저장됨): 백엔드 ContractRequest 에 @AssertTrue isEndDateValid() 교차검증(둘 다 있을 때 endDate>=startDate) → 400. 프론트 ContractFormView.onSubmit 에 기간 비교 가드(사전식 비교).
  · P2-1(수정 응답 updatedAt 이 갱신 전 값): ContractService/ClientService update()에서 saveAndFlush 로 @PreUpdate(updatedAt) 반영 후 DTO 생성.
  · P2-2(수정폼 watchEffect 가 await 이후 props.id 를 의존으로 못 잡음): watch(() => props.id, ..., {immediate:true})로 전환 — Contract/Client 폼 둘 다.
  · P3-1(페이징)·P3-2(billingDay 월말)·P3-3(낙관적 락)은 리뷰 권고대로 보류(정산 도입/설계·다중관리자 시).
- 변경 파일: `contract/dto/ContractRequest`·`contract/service/ContractService`·`client/service/ClientService`, 프론트 `views/admin/contract/ContractFormView.vue`·`views/admin/client/ClientFormView.vue`, 리뷰 문서 상태 갱신
- 검증: clean compile + vite build SUCCESS. 실기동 E2E — 종료일<시작일 등록/수정 각각 400, 정상 등록/수정 통과, 수정 응답 updatedAt 이 이번 수정 시각으로 갱신됨 확인.
- 관련: P1-1 은 커밋 ab76e24 에서 이미 처리.

### [리팩터] FileUtillMo 파일 다운로드 보완 + 유틸 인자 스타일 일치
- 조치: 파일 관련 유틸(FileUtillMo)을 정리.
  · 레거시 downloadFile 보완: (1) 항상 1024 write→파일 손상 버그를 실제 읽은 만큼 write 로 수정, (2) 브라우저 판별(getDocNameByBrowser) 대신 RFC 5987(UTF-8) 표준 파일명 인코딩(contentDisposition 헬퍼), (3) 콘텐트타입 probe null 시 octet-stream fallback, (4) try-with-resources 로 자원 정리(빈 catch 제거). 두 오버로드를 공통 streamToResponse 로 위임, 미사용 request 파라미터 제거. getDocNameByBrowser는 @Deprecated 표시(사용처 없음).
  · downloadResponse 인자 스타일 일치: uploadSingleFile(baseDir,target)·downloadFile(dirPath,filePath) 과 맞춰 `downloadResponse(baseDir, relativePath, filename, contentType)` 경로 오버로드 추가(내부에서 readBytes 후 byte[] 오버로드에 위임). 계약 첨부 다운로드가 이걸 사용(서비스 download() 로 합치고 컨트롤러 1줄로 축소).
  · 파일 전체가 탭/스페이스 혼용(CLAUDE.md 위반)이라 4-space 로 통일(로직 변경은 downloadFile 관련뿐).
- 변경 파일: `util/file/FileUtillMo`, `contract/controller/ContractAttachmentController`, `contract/service/ContractAttachmentService`
- 검증: clean compile SUCCESS. 실기동 E2E — 계약 첨부 업로드/다운로드 정상(2000~5000B 등 1024 비배수 포함 바이트 일치)·한글 파일명·콘텐트타입, 없는 첨부 404.

### [기능] 계약서 파일 첨부/관리 — 파일시스템 저장 + FileUtillMo 일원화
- 배경: 실무 계약의 원본은 수기 계약서(종이)나 파일이라, 계약 도메인에 이를 담을 곳이 필요했다.
- 조치:
  · 보관 위치 메모: contract.document_location(종이 원본 등 실물 위치) 추가.
  · 첨부 파일: ContractAttachment(계약 1:N), 업로드(확장자 화이트리스트)·목록(메타 프로젝션)·다운로드·삭제. 관리자 API `/api/admin/contracts/{id}/attachments`.
  · 저장 방식: 처음 DB(bytea)로 구현했다가, 사용자 요청으로 **파일시스템 저장**으로 전환(설정 `file.upload-dir`, 개발 ./uploads/gitignore, 운영 env FILE_UPLOAD_DIR). DB엔 메타+저장 상대경로(stored_path)만. 계약 삭제 시 첨부 물리파일도 정리(FK cascade는 행만 지우므로 파일은 서비스가 함께 삭제).
  · 파일 I/O 일원화: 파일 관련은 util/file/FileUtillMo로 모음. `uploadSingleFile`을 버그(실패해도 true 반환·PBox/boolean) 고쳐 경로반환+예외로 재사용(중복 `storeMultipart` 제거), 다운로드는 `readBytes`+신규 표준 `downloadResponse`(콘텐트타입=저장값, 파일명 UTF-8 퍼센트인코딩) 사용. 레거시 `downloadFile`의 "항상 1024 write→파일 손상" 버그도 수정(실제 읽은 만큼만 write). blob↔파일 브리지 `writeBytes`/`readBytes` 추가.
  · 부가: multipart 상한 초과(MaxUploadSizeExceededException)→413 처리(GlobalExceptionHandler), ErrorCode FILE_TOO_LARGE(010003)·CONTRACT_ATTACHMENT_NOT_FOUND(100102)+i18n. 프론트: 계약 폼에 보관위치 필드, 상세에 첨부 섹션(업로드/목록/다운로드/삭제, 기존 downloadGet 재사용).
- 변경 파일: `contract/domain/{Contract,ContractAttachment}`·`contract/dto/{ContractRequest,ContractResponse,ContractAttachmentResponse}`·`contract/repository/{ContractRepository,ContractAttachmentRepository}`·`contract/service/{ContractService,ContractAttachmentService}`·`contract/controller/ContractAttachmentController`, `util/file/FileUtillMo`, `error/{ErrorCode,GlobalExceptionHandler}`, `etc.properties`·i18n(ko/en), `db/migration/{V5__add_contract_document,V6__contract_attachment_to_filesystem}.sql`, `.gitignore`, 프론트 `views/admin/contract/{ContractFormView,ContractDetailView}.vue`·`services/contract/contractService.js`
- 검증: clean compile SUCCESS + vite build SUCCESS. 실기동 E2E — V5/V6 적용·validate 통과, 업로드 시 디스크 파일 생성, 다운로드 바이트 일치(5000B 등 1024 비배수 포함 손상 없음)·한글 파일명 헤더·콘텐트타입 정상, 확장자 불허 400·용량초과 처리, 첨부/계약 삭제 시 물리파일 정리.
- 관련: PBox/PBoxList 컨벤션 확정(범용 Map/ArrayList 자리만 사용, 타입 도메인 DTO·List<Dto>는 JPA+DTO 유지) — 메모리에 기록.

### [수정] 거래처↔계약 연결 + 계약 걸린 거래처 삭제 방지(회귀 P1-1)
- 증상: (P1-1) 계약이 걸린 거래처를 삭제하면 안내가 아니라 500(FK 위반 미처리) — 계약 도메인 추가로 생긴 회귀. 별도 코드리뷰 문서(review-contract-2026-07-08.md)가 지적.
- 조치:
  · P1-1: ClientService.delete()에서 계약 존재 검사(ContractRepository.countByClientId>0 이면 CLIENT_HAS_CONTRACTS(100002, 409) 안내, 삭제 금지 정책 — 정산 이력 보존). i18n 추가.
  · 연결: 거래처 상세에 "이 거래처의 계약" 목록 + "계약 추가"(계약 폼 ?clientId= 프리셀렉트) + 계약 클릭 시 상세 이동.
- 변경 파일: `client/service/ClientService`·`contract/repository/ContractRepository`·`error/ErrorCode`·i18n, 프론트 `views/admin/client/ClientDetailView.vue`
- 검증: 실기동 E2E — 계약 있는 거래처 삭제 409, 없는 거래처 200, 계약 정리 후 200. 거래처 필터 조회 정상.

### [수정] 계약/거래처 관리 UI 개선 3건 (실사용 피드백)
- 증상/조치:
  · (1) 월 청구금액에 2300 입력 시 등록 불가 → 입력칸 `step="1000"`이 1000 배수만 유효로 판정한 탓. `step="1"`로 변경(임의 정수 허용, 덤으로 소수 입력을 브라우저가 차단). `ContractFormView.vue`.
  · (2) 수정 후 상세 페이지에 즉시 반영 안 됨(새로고침해야 보임) → 저장 시 목록 캐시(`['contracts']`)만 무효화하고 상세 캐시(`['contract', id]`)는 안 해 vue-query가 staleTime(30s) 동안 옛 캐시 노출. 저장 후 상세 키도 무효화 추가(접두어 매칭). 같은 버그인 거래처 폼도 동일 수정. `ContractFormView.vue`·`ClientFormView.vue`.
  · (3) 목록에서 이름 글자만 링크였음 → 행(tr) 전체 클릭으로 상세 진입하게 통일. `@click`으로 이동, 수정/삭제 칸은 `@click.stop`으로 제외, `row-click` 커서 포인터. 계약·거래처 목록 둘 다 적용. `ContractListView.vue`·`ClientListView.vue`.
- 변경 파일: `frontend/vue/src/views/admin/{contract/ContractFormView.vue,contract/ContractListView.vue,client/ClientFormView.vue,client/ClientListView.vue}`
- 검증: vite build SUCCESS(각 수정마다 재빌드, static/app 반영). 사용자 실사용 확인.

### [기능] 계약(contract) 도메인 추가 — 관리자 CRUD + 거래처 1:N 참조
- 조치: 두 번째 도메인 수직 슬라이스(백엔드→프론트). 거래처(client) 템플릿을 그대로 따라 구현. 정기 청소 월정액 계약을 관리.
  · 설계 확정: 거래처 1:N 계약(한 건물에 재계약 이력 등 여러 계약), 필드 = 거래처(FK)·계약명·월청구금액(원)·청구일(1~31)·시작일·종료일(nullable)·상태(ACTIVE/ENDED/SUSPENDED)·메모.
  · 백엔드(`com.boot.cleanhub.contract`): Contract 엔티티(@ManyToOne(LAZY)→Client)·ContractStatus enum·Flyway V4__create_contract.sql(client_id FK+인덱스)·ContractRepository(fetch join 으로 client 동반 조회 → 목록/검색/거래처필터/단건, N+1 회피)·ContractService(CRUD·거래처 존재검증·status 기본 ACTIVE)·DTO(요청 검증/응답에 clientId+clientName+statusLabel 평면화)·ContractAdminController(/api/admin/contracts, ROLE_ADMIN 보호)·ErrorCode.CONTRACT_NOT_FOUND(100101)+i18n(ko/en).
  · 프론트: contractService(list/get/create/update/remove, CONTRACT_STATUSES)·목록(vue-query 캐싱·계약명 검색·월금액 원단위 표시·상태 tag)·등록/수정 폼(거래처 셀렉트, ?clientId= 프리셀렉트, 월금액/청구일 number)·상세(거래처 링크·무기한 표기)·라우터 /admin/contracts** 5개·사이드바/대시보드 카드 추가.
  · 거래처 엔티티의 기존 초안 필드(cleaningType·contractStartDate)는 이번엔 미변경(실무 피드백 후 정리 예정).
- 변경 파일: `contract/*`(신설 7: domain 2·repository·dto 2·service·controller), `db/migration/V4__create_contract.sql`, `error/ErrorCode`·i18n(ko/en), `frontend/vue/src/{services/contract,views/admin/contract(신설 3),router/index.js,layouts/AdminLayout.vue,views/admin/AdminDashboardView.vue}`
- 검증: clean compile SUCCESS(97 소스) + vite build SUCCESS. 실기동(:7071) — Flyway V4 적용(스키마 v4)·JPA validate 통과. API E2E 전체 통과: 로그인→JWT→거래처생성→계약 등록/단건/목록/검색(계약명)/거래처필터/수정/삭제/삭제후404, 검증실패 400(월금액 누락), 없는 거래처 404(CLIENT_NOT_FOUND).
- 관련: 거래처(client) 도메인이 본보기(같은 계층·컨벤션). 로드맵상 다음은 견적(quote) 또는 정산(settlement).

### [기능] 자동로그인 — refreshToken을 HttpOnly 쿠키로 전환 (세션 앵커 + JWT API + refresh 쿠키)
- 증상: JWT access·refresh 를 프론트 메모리에만 보관 → 새로고침·브라우저 재시작 시 소실 → 관리자 페이지에서 로그인 화면으로 튕김.
- 원인: 토큰 보관이 메모리(JS 변수)라 페이지 리셋 시 사라짐. 라우터 가드는 토큰 없으면 로그인으로 리다이렉트.
- 조치: 인증 구조를 "세션(HttpOnly, 브라우저 열린 동안 로그인상태) + access(JWT, 메모리, /api Bearer) + refresh(HttpOnly 쿠키 + DB, 자동로그인 장기 신분증)"로 정리.
  · 백엔드: `RefreshTokenCookie`(support) 신설 — HttpOnly·SameSite=Lax·Secure(설정값)·Path=/, rememberMe=true면 Max-Age=7일/false면 세션쿠키. `/auth/api/token`은 refresh를 쿠키로 발급(body엔 access·username·roles만, `AuthResult` DTO). `/api/auth/refresh`는 @CookieValue로 refresh 읽어 DB대조+파싱검증→새 access(회전 시 새 쿠키), **세션 불필요=자동로그인**. `/api/auth/logout`은 쿠키 Max-Age=0 삭제+DB삭제. etc.properties `jwt.refresh-cookie.secure`(운영 env REFRESH_COOKIE_SECURE).
  · 프론트: axios `withCredentials`(쿠키 전송), authService 쿠키기반(refresh body 제거), auth 스토어 refreshToken 메모리 제거·rememberMe 기억·`restore()`/`applyAuth`, main.js 앱시작 시 restore()로 복원(라우터 가드보다 먼저), 관리자 로그인 화면 "로그인 유지" 체크박스.
- 변경 파일: `auth/support/RefreshTokenCookie`·`auth/dto/AuthResult`(신설), `auth/controller/AuthApiController`·`TokenRefreshController`, `etc.properties`, `frontend/vue/src/{main.js,plugins/http/axios.js,services/auth/authService.js,stores/auth/auth.js,views/admin/AdminLoginView.vue}`
- 검증: 실기동(:7071) E2E — 발급 시 HttpOnly Set-Cookie(body에 refresh 없음), 세션 없이 쿠키만으로 갱신 성공(재시작 시나리오)+회전 재발급, 갱신 access로 /api/admin 200, 로그아웃 시 쿠키 삭제+DB삭제→이후 갱신 401, 쿠키 없이 갱신 401. vite build SUCCESS.
- 관련: WEB_PORTAL(refresh 기반 자동로그인) 분석 참고 — 같은 철학이되 HttpOnly로 한 단계 더 안전(XSS로 못 훔침), 회전+DB검증으로 폐기 가능. 커밋 e6c450e.

### [기능] 거래처(client) 도메인 추가 — 관리자 CRUD + 대민/관리자 영역 분리
- 조치: 첫 도메인 수직 슬라이스(백엔드→프론트, 이후 도메인의 템플릿).
  · 백엔드(`com.boot.cleanhub.client`): Client 엔티티(건물명·주소·담당자·청소종류·계약시작일·메모+생성/수정시각)·CleaningType(REGULAR/SPECIAL)·Flyway V3__create_client.sql·ClientRepository(건물명 검색)·ClientService(CRUD·트랜잭션)·DTO(요청 검증/응답 라벨)·ClientAdminController(/api/admin/clients, ROLE_ADMIN 보호)·ErrorCode.CLIENT_NOT_FOUND. AuthApiController.me() authorities 문자열→roles 배열.
  · 프론트: 레이아웃 PublicLayout(대민 상단바)·AdminLayout(관리자 사이드바), 라우터 /admin/** ROLE_ADMIN 가드+중첩레이아웃, 관리자 로그인/대시보드, 거래처 목록(vue-query 캐싱·검색·삭제)·등록/수정 폼·상세(teal 톤, 폼/상세 가운데정렬), auth 스토어 roles·isAdmin, style.css 디자인토큰.
- 증상/수정: SPA 루트 진입(/app, /app/) 404 — ResourceHttpRequestHandler가 빈 하위경로를 리졸버 전에 404 처리 → WebConfig addViewController `forward:/app/index.html`로 보정, auth.login-redirect 기본값 /app→/app/.
- 변경 파일: `client/*`(신설 8), `config/WebConfig`·`error/ErrorCode`·`auth/controller/AuthApiController`, `db/migration/V3__create_client.sql`, i18n, `frontend/vue/src/{layouts,views/admin,services/client,router,stores/auth,style.css}` 등
- 검증: clean compile + 실기동 — Flyway V3 적용, 거래처 CRUD E2E(등록/목록/검색/수정/삭제·404·401) 통과, me() roles 배열 확인, /app/ 진입 200. vite build SUCCESS. 커밋 711ad82.

## 2026-07-07

### [환경] CleanHub 초기 세팅 — head 프레임워크 파생, 데모 제거, PostgreSQL+Flyway
- 조치: `spring_boot_head_mvn`(범용 헤드)을 복사해 청소 견적·정산 관리 시스템 `cleaning-service`(repo) 착수.
  · 데모 제거: 프론트 _demo(crud·excel·query-demo), 백엔드 test/testcase 전체(100파일)·templates/testcase·데모 mybatis 매퍼·H2용 schema/data.sql·PKCE(jwt_user) 배선.
  · 패키지 com.boot.project→com.boot.cleanhub(앱/테스트 클래스명 포함), artifactId cleanhub.
  · DB H2→PostgreSQL 18, Flyway로 스키마 관리(ddl-auto validate). V1__init(auth_user·jwt_refresh_token)·V2__seed_admin(admin/admin1234, pgcrypto crypt() BCrypt).
  · 유지: 인증(세션+JWT 이중체인)·config·filter·util·error·common(api·dao·dto·handler·mail·notification·aop)·logback-spring.xml·i18n. 프론트 Vue3 스타터(auth·home·notify·i18n·axios·vue-query).
- 증상/수정: `${username}` 플레이스홀더가 Windows 환경변수 USERNAME(=OS사용자)과 충돌해 DB 접속 실패 → db.properties 키에 `db.` 접두사로 네임스페이스 분리.
- 변경 파일: 전 범위(초기 커밋). CLAUDE.md/README.md cleanhub 기준 재작성.
- 검증: clean package SUCCESS(cleanhub-0.0.1-SNAPSHOT.war), 실기동 — Flyway V1·V2 적용, admin 로그인→JWT 발급 E2E 통과. 커밋 3a110c5.
- 관련: 로컬 DB 셋업(cleanhub DB·cleanhub_user·pgcrypto 확장)은 CLAUDE.md/README 참조.
